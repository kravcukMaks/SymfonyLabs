<form method="post" action="{{ path('app_library_branch_delete', {'id': library_branch.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ library_branch.id) }}">
    <button class="btn">Delete</button>
</form>
